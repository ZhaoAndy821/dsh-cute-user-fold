/**
 * dsh-cute-user-fold · 宿主侧（host half）
 *
 * 本插件的全部工作都在浏览器侧完成：「折叠用户消息」是渲染层的表现问题，
 * 宿主侧不需要任何参与 —— 不需要新的 RPC、不需要读写会话数据、不需要配置项。
 *
 * 这里**刻意保持为空实现，但必须存在** —— `cordis.patch.yml` 里的挂载行会让
 * 加载器解析本包的**主入口**（`main: lib/index.js`）。缺了它，插件树会在启动时
 * 报「无法解析入口」，整棵插件树加载失败。也就是说：空 ≠ 可省。
 */

/**
 * 宿主侧插件主体：不做任何事。
 *
 * 保留显式空函数而非删掉文件，是为了让「宿主侧有意为空」这件事在代码里可见 ——
 * 下一位读者不必再去猜是不是漏写了。
 */
export function apply() {
  // 有意为空：折叠是纯客户端行为，宿主侧无工作要做。
}
